[tool.poetry]
name = "socks5-tune"
version = "0.1.0"
description = ""
license = "MIT"
authors = ["B7W <b7w@isudo.ru>"]

[tool.poetry.dependencies]
python = "^3.13"
sanic = "^25.12"
python-socks = {version = "^2.7", extras = ["asyncio"]}
sqlalchemy = "^2.0"
psycopg = {version = "^3.3", extras = ["binary"]}
msgspec = "^0.20"

[tool.poetry.group.dev.dependencies]
pytest = "^8"

[tool.poetry.scripts]
socks5-tune = 'socks5_tune.main:main'

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
